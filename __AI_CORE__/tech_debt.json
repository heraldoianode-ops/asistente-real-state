{
  "debts": [
    {
      "id": "TD-006",
      "title": "Legacy FastAPI backend unused by dashboard",
      "severity": "LOW",
      "detected": "2026-06-10",
      "status": "RESOLVED",
      "resolved": "2026-06-21",
      "resolution_es": "Auditado: ningún servicio en producción (dashboard Netlify ni whatsapp-gateway slim) consume el backend. Archivado en remoto como rama 'archive/legacy-fastapi-backend' @ a41aba6 (el proxy git del entorno rechaza push de tags con 403; existe además el tag local 'legacy-fastapi-backend'). Eliminados del árbol: backend/, gateway/ (viejo), docker-compose.yml, dashboard/src/lib/api.ts (muerto) y rewrite de next.config. Decisión D016.",
      "resolution_en": "Audited: no production service (Netlify dashboard nor slim whatsapp-gateway) consumes the backend. Archived on the remote as branch 'archive/legacy-fastapi-backend' @ a41aba6 (the environment git proxy rejects tag pushes with 403; a local tag 'legacy-fastapi-backend' also exists). Removed from tree: backend/, gateway/ (old), docker-compose.yml, dead dashboard/src/lib/api.ts and the next.config rewrite. See decision D016.",
      "affected": ["backend/"],
      "description_es": "El backend FastAPI completo sigue en el repo pero el dashboard usa supabase-js directo y Edge Functions desde la Fase 8. Su capa de auth no aplica scoping multi-tenant. Candidato a borrado o archivado tras confirmar que ningún servicio (gateway, cron, scraper) lo consume.",
      "description_en": "Full FastAPI backend remains in the repo but the dashboard uses supabase-js directly and Edge Functions since Fase 8. Its auth layer lacks multi-tenant scoping. Candidate for deletion or archiving after confirming no service (gateway, cron, scraper) consumes it.",
      "proposed_fix_es": "Auditar consumidores → archivar o eliminar backend/ → limpiar docker-compose y ENV asociadas.",
      "proposed_fix_en": "Audit consumers → archive or delete backend/ → clean docker-compose and related ENV vars."
    },
    {
      "id": "TD-007",
      "title": "Schema drift — repo migrations don't reflect live users table",
      "severity": "LOW",
      "detected": "2026-06-21",
      "status": "RESOLVED",
      "resolved": "2026-06-21",
      "affected": ["db/init/02_schema.sql", "db/migrations/04_rls_policies.sql"],
      "description_es": "La tabla 'users' en producción tiene 'auth_user_id' (uuid) y ya NO tiene 'hashed_password', pero ninguna migración versionada lo reflejaba. Peor aún: la migración 05 referencia users.auth_user_id en políticas de storage, por lo que una build limpia fallaba (la columna no existía hasta ese punto). 'avatar_url' SÍ estaba cubierto por 05 (no era drift).",
      "description_en": "The production 'users' table has 'auth_user_id' (uuid) and no longer has 'hashed_password', but no versioned migration reflected it. Worse: migration 05 references users.auth_user_id in storage policies, so a clean build failed (the column did not exist yet). 'avatar_url' WAS already covered by 05 (not drift).",
      "resolution_es": "Añadido a 04_rls_policies.sql (antes de las políticas y de 05), idempotente: ADD COLUMN IF NOT EXISTS auth_user_id UUID; DROP COLUMN IF EXISTS hashed_password. Verificado contra producción vía information_schema: coincide (no-op en prod).",
      "resolution_en": "Added to 04_rls_policies.sql (before the policies and before 05), idempotent: ADD COLUMN IF NOT EXISTS auth_user_id UUID; DROP COLUMN IF EXISTS hashed_password. Verified against production via information_schema: matches (no-op on prod).",
      "proposed_fix_es": "Migración idempotente de reconciliación ubicada antes de las dependencias.",
      "proposed_fix_en": "Idempotent reconciliation migration placed before its dependents."
    },
    {
      "id": "TD-008",
      "title": "RLS policies key off users.id = auth.uid() but app links via users.auth_user_id",
      "severity": "MEDIUM",
      "detected": "2026-06-21",
      "status": "RESOLVED",
      "resolved": "2026-06-22",
      "affected": ["db/migrations/04_rls_policies.sql", "supabase/functions/create-agent/index.ts"],
      "description_es": "Sospecha (contra el repo): is_admin() y el scoping usarían 'id = auth.uid()' mientras la app enlaza por 'auth_user_id', rompiendo el scoping para agentes con id != auth_user_id.",
      "description_en": "Suspicion (against the repo): is_admin() and scoping would use 'id = auth.uid()' while the app links via 'auth_user_id', breaking scoping for agents whose id != auth_user_id.",
      "resolution_es": "FALSO en producción. Un trigger BEFORE INSERT (enforce_user_id_equals_auth) fuerza users.id := auth_user_id, por lo que id == auth_user_id == auth.uid() para todos. Además prod usa el helper current_agent_id() para el scoping. No hay bug. La discrepancia era del repo (migración 04 vieja), reconciliado en 07. Decisión D017. Sin cambios en prod.",
      "resolution_en": "FALSE in production. A BEFORE INSERT trigger (enforce_user_id_equals_auth) forces users.id := auth_user_id, so id == auth_user_id == auth.uid() for everyone. Prod also scopes via the current_agent_id() helper. No bug. The discrepancy was in the repo (stale migration 04), reconciled in 07. Decision D017. No prod changes.",
      "proposed_fix_es": "—",
      "proposed_fix_en": "—"
    },
    {
      "id": "TD-009",
      "title": "Repo↔prod drift: RLS, triggers and Google integration only in production",
      "severity": "MEDIUM",
      "detected": "2026-06-22",
      "status": "RESOLVED",
      "resolved": "2026-06-22",
      "affected": ["db/migrations/", "__AI_CORE__/features.json"],
      "description_es": "Producción evolucionó fuera de las migraciones: is_admin()/current_agent_id, trigger de identidad, set_updated_at en 11 tablas, RLS consolidado, y la integración Google (agent_google_credentials, google_oauth_states) — nada en el repo ni en features.json. Un entorno recreado desde el repo no reproduciría prod.",
      "description_en": "Production drifted out of migrations: is_admin()/current_agent_id, identity trigger, set_updated_at on 11 tables, consolidated RLS, and the Google integration (agent_google_credentials, google_oauth_states) — none in the repo or features.json. A rebuild from the repo would not reproduce prod.",
      "resolution_es": "Capturado el estado vivo en migración idempotente 07_reconcile_with_prod.sql; F026 registrada; ver docs/AUDIT-2026-06-22.md. No-op sobre prod.",
      "resolution_en": "Live state captured in idempotent migration 07_reconcile_with_prod.sql; F026 logged; see docs/AUDIT-2026-06-22.md. No-op on prod.",
      "proposed_fix_es": "—",
      "proposed_fix_en": "—"
    },
    {
      "id": "TD-010",
      "title": "Governance: changes applied directly to prod without versioned migrations",
      "severity": "LOW",
      "detected": "2026-06-22",
      "status": "OPEN",
      "affected": ["process"],
      "description_es": "La causa raíz de TD-007/008/009 fue aplicar DDL/RLS y features directo en Supabase sin migración versionada (viola R7). Riesgo recurrente de drift. Diff columna-por-columna repo↔prod completado 2026-06-23 (15 tablas en asistente_real_state): única columna faltante era events.google_event_id → añadida en migración 07; resto del esquema coincide. Pendiente: gobernanza de proceso.",
      "description_en": "Root cause of TD-007/008/009 was applying DDL/RLS and features directly in Supabase without a versioned migration (violates R7). Recurring drift risk. Column-level repo↔prod diff completed 2026-06-23 (15 tables in asistente_real_state): only missing column was events.google_event_id → added in migration 07; rest of the schema matches. Remaining: process governance.",
      "proposed_fix_es": "Disciplina: toda evolución de esquema/RLS entra primero como migración en db/migrations/ y luego se aplica; auditoría periódica repo↔prod.",
      "proposed_fix_en": "Discipline: every schema/RLS change lands first as a migration in db/migrations/ then is applied; periodic repo↔prod audit."
    },
    {
      "id": "TD-011",
      "title": "Edge Function drift between repo and deployed prod",
      "severity": "MEDIUM",
      "detected": "2026-06-22",
      "status": "OPEN",
      "affected": ["supabase/functions/"],
      "description_es": "Prod despliega solo create-agent + wa-gateway. (1) wa-gateway no estaba en el repo → AÑADIDO en supabase/functions/wa-gateway/index.ts. (2) match-properties y notify-agent existen en el repo pero NO están desplegadas → F021 (matches cross-agent) no está viva. (3) No existe Edge Function del flujo OAuth de Google → F026 es solo esquema (parcial).",
      "description_en": "Prod deploys only create-agent + wa-gateway. (1) wa-gateway was not in the repo → ADDED at supabase/functions/wa-gateway/index.ts. (2) match-properties and notify-agent exist in repo but are NOT deployed → F021 (cross-agent matches) is not live. (3) No Edge Function implements the Google OAuth flow → F026 is schema-only (partial).",
      "proposed_fix_es": "Decidir por feature: desplegar match-properties/notify-agent si F021 debe estar viva; implementar+desplegar la función OAuth de Google para completar F026; o marcar explícitamente como diferidas.",
      "proposed_fix_en": "Decide per feature: deploy match-properties/notify-agent if F021 should be live; implement+deploy the Google OAuth function to complete F026; or explicitly defer them."
    },
    {
      "id": "TD-012",
      "title": "wa-gateway /buscar queries non-existent properties.is_active → search errors",
      "severity": "MEDIUM",
      "detected": "2026-06-23",
      "status": "OPEN",
      "affected": ["supabase/functions/wa-gateway/index.ts"],
      "description_es": "El comando /buscar filtra properties con .eq('is_active', true), pero la tabla properties NO tiene columna is_active (usa 'status', default 'available'). PostgREST devuelve error → el bot responde 'Error al buscar'. La verificación de número registrado (whatsapp_numbers.is_active, que sí existe) funciona; la búsqueda de propiedades no. Detectado en el diff columna-por-columna repo↔prod. El código del repo es fiel a prod (mismo bug desplegado).",
      "description_en": "The /buscar command filters properties with .eq('is_active', true), but the properties table has NO is_active column (it uses 'status', default 'available'). PostgREST errors → the bot replies 'Error al buscar'. The registered-number check (whatsapp_numbers.is_active, which does exist) works; property search does not. Found via the column-level repo↔prod diff. Repo code is faithful to prod (same deployed bug).",
      "proposed_fix_es": "Reemplazar .eq('is_active', true) por .eq('status', 'available') en la consulta a properties y redesplegar wa-gateway (toca prod → requiere OK del usuario).",
      "proposed_fix_en": "Replace .eq('is_active', true) with .eq('status', 'available') in the properties query and redeploy wa-gateway (touches prod → requires user OK)."
    }
  ]
}
