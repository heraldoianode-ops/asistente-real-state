{
  "reinforced_checks": [
    { "id": "P001", "active": true, "rule": "Never commit .env or *.private.json" },
    { "id": "P002", "active": true, "rule": "All new endpoints require JWT auth unless explicitly public" },
    { "id": "P003", "active": true, "rule": "AI_POLICY env var controls LLM selection — never hardcode model" },
    { "id": "P004", "active": true, "rule": "pgvector queries must have ivfflat index and cosine distance" },
    { "id": "P005", "active": true, "rule": "Celery tasks must be idempotent" },
    { "id": "P006", "active": true, "rule": "RAG chunks: size=1800, overlap=360 (20%)" }
  ]
}
