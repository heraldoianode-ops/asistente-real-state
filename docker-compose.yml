version: '3.9'

services:
  dashboard:
    build: ./dashboard
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3000"

  whatsapp-gateway:
    build: ./whatsapp-gateway
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
